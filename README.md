
  # Mobile Food Ordering App

  This is a code bundle for Mobile Food Ordering App. The original project is available at https://www.figma.com/design/u4NoWMcwlCPZEGk5rutAPK/Mobile-Food-Ordering-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # mobile-food-ordering-prototype
